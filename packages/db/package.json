{
  "name": "@unstatus/db",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "prisma:generate": "bun --env-file=../../.env.local prisma generate --config prisma.config.ts",
    "prisma:migrate:deploy": "bun --env-file=../../.env.local prisma migrate deploy --config prisma.config.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.6.0",
    "@prisma/client": "^7.6.0"
  },
  "devDependencies": {
    "prisma": "^7.6.0",
    "tsx": "^4.21.0"
  }
}
