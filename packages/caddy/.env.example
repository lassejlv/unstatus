# Postgres connection string for domain lookups
DATABASE_URL=postgresql://user:password@host:5432/unstatus

# Railway app URL to proxy custom domain traffic to
UPSTREAM_URL=https://unstatus.up.railway.app

# Port for the internal ask/health API (default: 3456)
API_PORT=3456

# Optional shared secret for the ask endpoint
ASK_SECRET=
