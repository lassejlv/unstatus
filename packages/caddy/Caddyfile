{
	on_demand_tls {
		ask http://localhost:{$API_PORT:3456}/ask?secret={$ASK_SECRET}
	}
}

https:// {
	tls {
		on_demand
	}

	reverse_proxy {$UPSTREAM_URL} {
		header_up Host {upstream_hostport}
		header_up X-Forwarded-Host {host}
		header_up X-Real-IP {remote_host}
	}
}
