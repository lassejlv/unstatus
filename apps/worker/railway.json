{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd ../.. && bun install --frozen-lockfile && bun run db:generate && bun run --filter worker build"
  },
  "deploy": {
    "startCommand": "./worker",
    "sleepApplication": false,
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 67
  }
}
